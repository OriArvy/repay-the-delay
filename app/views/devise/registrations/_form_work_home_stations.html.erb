<%= simple_form_for current_user, url: user_registration_path, method: :patch do |f| %>
  <%= f.input :home_station %>
  <%= f.input :work_station %>
  <%= f.input :current_password %>
  <%= f.submit %>
<% end %>

<!-- In case we want to use it on separate page. -->
